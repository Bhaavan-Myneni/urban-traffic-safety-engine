# Default detection and pipeline settings
detection:
  model: models/yolov8n.pt
  confidence: 0.5
  iou: 0.45
  classes:
    - person
    - car
    - truck
    - bus
    - motorcycle
    - bicycle

pipeline:
  frame_sample_rate: 30
  batch_size: 16

database:
  pool_size: 5
  echo: false
